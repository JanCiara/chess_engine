#pragma once
#include "board.h"

inline constexpr int P_val = 100;
inline constexpr int N_val = 300;
inline constexpr int B_val = 350;
inline constexpr int R_val = 500;
inline constexpr int Q_val = 1000;
inline constexpr int K_val = 10000;

int evaluate(Board* board);
